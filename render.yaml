services:
  # Backend API Service
  - type: web
    name: garage-manager-api
    env: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node api/index.js"
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/vehicles
    autoDeploy: true
